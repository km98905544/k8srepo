apiVersion: v1 
kind: Service 
metadata:
  name: mynginx-svc
  labels: 
     app: myjavaapp
spec:
  type: NodePort 
  ports:
  - port: 8080
    NodePort: 30001
    protocol: TCP 
  selector:
    app: myjavaapp 
